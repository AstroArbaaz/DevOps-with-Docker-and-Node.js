# Compose commands

Following are the list of commands to work with

## Run Docker Compose

### build image

```bash
docker-compose up -d
```

- "-d" to run in detached mode

### delete image

```bash
docker-compose down -v
```

